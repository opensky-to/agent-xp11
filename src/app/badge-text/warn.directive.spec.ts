import { WarnDirective } from './warn.directive';

describe('WarnDirective', () => {
  it('should create an instance', () => {
    const directive = new WarnDirective();
    expect(directive).toBeTruthy();
  });
});
