import { StylingDirective } from './styling.directive';

describe('StylingDirective', () => {
  it('should create an instance', () => {
    const directive = new StylingDirective();
    expect(directive).toBeTruthy();
  });
});
